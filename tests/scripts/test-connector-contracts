#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
export PYTHONPATH="$ROOT/connectors/common/src${PYTHONPATH:+:$PYTHONPATH}"
exec pytest "$ROOT/tests/unit/test_connector_client.py" "$@"
