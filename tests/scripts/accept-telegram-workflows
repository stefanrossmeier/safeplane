#!/usr/bin/env bash
set -euo pipefail

cd "$(git rev-parse --show-toplevel)"

PYTHONPATH=services/harness/src:connectors/telegram/src \
python3 -m pytest -q \
  tests/acceptance/test_telegram_workflow_commands.py
