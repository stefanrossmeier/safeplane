#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
echo "WARNING: scripts/safeplane-chat is deprecated; use scripts/safeplane." >&2
exec "$SCRIPT_DIR/safeplane" "$@"
